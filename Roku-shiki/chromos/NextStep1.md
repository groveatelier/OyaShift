## 親指シフトキー入力の進化

Mu基本的にキーダウン時に入力キーの確定を実施する。  
    M 文字入力とシフト入力はズレるので、先の入力で確定してしまった文字は、  
    後から差し替える動作  
    をする。M キーダウン  lti-tap式のキー入力の開発を実施して見えてきたのは親指シフトキー入力の  キー日英入力のシームレスな切り替シフトキーを伴って確定した場合、次の文字入力に影響を与えないよう  
    シフトキーの不感帯を設ける。  る。  
英大文字もしくはMulti-tapでの英モードへの遷移、英モードで複数文字入力  
することで、英入力への完全遷移を実施。日モードへの復帰はMulti-Tapで  
実現する。  
更なる進化形態の開発です。  
親指シフトにマルチタップと長押し判断を持ち込む事によってキー入力に進化を  
促したいと考えています。

## 入力仕様のブレインストーミング

Multi-tapと長押しがどう活かされるか検討してみる。

- Multi-tap，長押しを英日入力切り替えに使う事で既存の切り替えとは  
異なってより自然に、誤爆もない切り替えを実現させる。
- JISキーの場合は、スペースでの変換を止めてタブキーのみの変換にし  
誤爆を抑えた上で、長押しによる濁音入力、スペース同時押しによるシフト  
を実現させる。
- 親指シフトキーボードであっても長押しによる濁音入力は可能とする。
- Multi-tap方式は、KeyDownで文字確定を実施している。親指シフト  
方式にもKeyDownで確定させる方式を取り入れる。  
- スペースキー押下中にMulti-tapがあったかを判断し入力のバリエーション  
に使えないか。→ 日英切り替えとか。

## 実装検討のブレインストーミング

- JISキー用スペースによるシフトについて
    - スペースキーを押下しているかを判断するためには、スペースキーの  
    状態を記憶しておく変数を設けることで対応する。
    - スペースキーの押下でキー確定を行うのはlinebufが空の場合のみと  
    他のキーとは違う特別判断が必要。→ 判断を容易にするためにかな  
    テーブルの先頭にスペースを配置する。
    - スペースキーの役割りをスペース，シフト，変換と多機能にした場合は  
    誤判断の可能性が高くなる。  
    スペース入力はinsidebufが空の条件で判別可能。 linebufに文字が存在する  
    場合はシフトと変換の判断が必要となる。  
        - insidebufが空の場合、  
        Timer割り込みでタイミングを作成し、割り込みのタイミングでアプリに  
        空白をおくる。
        またキーリピートのKeyDownと認識した場合は、KeyDown直後に  
        空白を送る。
        x一旦insidebufに空白を設定はするが後のKeyUpでアプリに空白を送る。 
        - insidebufに文字があり且つ文字offsetが0以外の場合、  
        変換候補の場合とシフトの先行入力の場合がある。
        Timer割り込みでタイミングを作成し、割り込みが入ったタイミングで  
        変換候補変更処理を行う。また、割り込み前に文字キーが入力された  
        場合は割り込みTimerをクリアし、文字入力を行う。
        - insidebufに文字があり且つ文字offsetが0の場合、
        insidebufの文字列と空白をアプリに吐き出す。 
- JIS&親指シフトキーボード共通に使える処理
    - 日モードの時にShiftキーと英文字キーを押下された場合は英モードに  
    切り替える。 記号キーの場合は日モードのままとする。
    - 日モードの時にスペースキー押下のまま英文字キーをMulti-tapした  
    場合は英モードに切り替える。 記号キーの場合は日モードのままとする。
    - 英モードの時にスペースキー押下のままかな文字キーをMulti-tapした  
    場合は日モードに切り替える。かな文字キー以外の場合は英モードの  
    ままとする。
- 英モードの場合もIME候補窓を使う場合
    - スペースキーが押された時にその前のキーオフセットが0の場合は  
    そこまでの文字列を出力して更にオフセット0の空白を確定する。  
    - 現状のUSモードが不要となるため、キー入力判断コードの大幅  
    リファクタリングが期待できる。  
    - 英モードでEnterを押された場合は、入力文字の確定に加え  
    Enterを送る方が良い。他に「.,?!」等の文字はその時点で  
    確定してもよい。
- スペースキーの押下中にMulti-tapがあったかを判断する方法
    - スペースキー押下の世代管理を行い文字キーのKeyDown時に世代の  
    比較を行い、同じ世代であればMulti-tapと判断する。
- スペースキー押下中のみMulti-tap機能を有効とする場合
    - 先ずスペースキーで空白確定，文字キーで空白取り消しの文字確定，  
    次の文字キーが同じ世代のスペースキー，尚且つ同じ文字キーの場合は  
    前の文字を削除後そのキーに属する文字を順に確定させる。 
    - スペースキーを押下した時点で空白を確定するので、キーリピートを  
    受けてしまうとMulti-tapより先に空白を完全確定してしまう。  
    スペースのリピートを初回KeyDownから暫く抑止する。  
    KeyDown時に既にスペースが押下中であれば初回キーDownからの  
    時間を判断してKeyDownの無効有効の処理分岐を行う。 
    

# 六式 コード的な進化

Frontend/Backend共にコードの刷新をやりたい。

## Frontendコードの刷新について

- 基本的にキーダウン時に入力キーの確定を実施する。  
    - 文字入力とシフト入力はズレるので、先の入力で確定してしまった  
    文字は、後から差し替える動作をする。  
    - シフトキーを伴って確定した場合、次の文字入力に影響を与えないよう  
    シフトキーの不感帯を設ける。  
- 日英入力のシームレスな切り替えを実現する。  
英大文字もしくはMulti-tapでの英モードへの遷移、英モードで複数文字入力  
することで、英入力への完全遷移を実施。日モードへの復帰はMulti-Tapで  
実現する。  
- クラスの責務について  
    - Senseクラス   
    キーイベントを受けてキー判定を行う。 キー情報の保持や Multi-tap  
    長押し，キー不感帯の処理を御する。
    - 文字Mapクラス  
    キーマップからの文字の検索を行う。

- curbufの更新タイミングについて
    - curbufはcomposition(カーソル位置に文字を表示する)の
    タイミングで設定される。   
    このタイミングでの設定はinbufをコピーか、dataアレイから選択し候補を
    元に作成される。（＊認識として）
    - 日本語入力中にスペースキーが押下された時、inbufには文字列が存在し   
    文字列の変換を成す処理に入る。この際空白を出力することは無いので、  
    curbufにも空白は設定しない。   
    日本語入力の初回とか、変換・確定が終わって新しく文字を入力する  
    タイミングでスペースキーが押下された場合は空白を出力する必要がある。  
    この場合inbufには既に空白が入力されているので、アプリに空白を  
    直接渡したならばinbufをクリアする必要がある。  
    inbufが空白のみの場合は seen を分けて処理も分ける。  
    - inbufが空白のみの場合の処理としてはJISキーの場合普通にある。   
    
